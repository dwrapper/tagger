# tagger
File tagger\viewer Qt application

## Flatpak deployment
Run `scripts/deploy-flatpak.sh` to build a Flatpak bundle in `build/tagger.flatpak`. The script uses `flatpak-builder` and pulls dependencies from Flathub.
